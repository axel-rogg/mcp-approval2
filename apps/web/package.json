{
  "name": "@mcp-approval2/web",
  "version": "0.0.1",
  "private": true,
  "description": "mcp-approval2 PWA — Login + Approval-Queue + WebAuthn-Sign (skeleton).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "drizzle-kit": "^0.31.10",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.0",
    "vite": "^8.0.13",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "dompurify": "^3.4.3",
    "highlight.js": "^11.11.1",
    "marked": "^15.0.12"
  }
}
