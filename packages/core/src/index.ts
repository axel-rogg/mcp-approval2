/**
 * @mcp-approval2/core — top-level re-export.
 *
 * For now this just forwards the crypto surface. Other primitives (errors,
 * structured-logger, etc.) will live alongside as the package grows.
 */

export * from './crypto/index.js';
